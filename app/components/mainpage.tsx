'use client';

import React, { FC } from "react";

const MainPage: FC = () => {
    return(
        <button onClick={() => window.location.href = '../MainPage'}>Aojjjjjjjj</button>
    );
}

export default MainPage;